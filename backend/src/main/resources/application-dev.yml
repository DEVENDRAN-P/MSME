spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    com.idbi.msme: DEBUG

app:
  cors:
    allowed-origins: http://localhost:5173,http://localhost:3000
  security:
    rate-limit:
      enabled: false
